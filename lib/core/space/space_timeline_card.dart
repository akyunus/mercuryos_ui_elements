import 'package:flutter/material.dart';

class SpaceTimelineCard extends StatelessWidget {
  const SpaceTimelineCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
